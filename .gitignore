# Built map assets — never commit these; they are published to R2.
/dist/
*.pmtiles

# Local planetiler working files
*.osm.pbf
planetiler.jar
data/

# Terrain build intermediates — the downloaded swissALTI3D GeoTIFFs, the warped
# raster and the flat grid. Tens of GB, and all of it reproducible from
# build-terrain.sh.
/work/

# Worker
worker/node_modules/
worker/.wrangler/

# Python
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.venv/
# No runtime dependencies, so there is nothing to lock.
uv.lock
